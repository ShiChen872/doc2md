# Prefer targeted extras: markitdown[all] can fail to resolve on newer Python
# (e.g. youtube-transcript-api conflicts on 3.14).
markitdown[docx,pptx,xlsx,pdf]>=0.1.0
pymupdf>=1.24.0
playwright>=1.40.0
office2pdf-python>=0.2.0
rapidocr-onnxruntime>=1.2.0
markdown>=3.6
pillow>=10.0.0
# Optional: `pip install typst` for --engine typst (Chrome remains the default PDF path).
